package com.tommyhasselman.termsconditions.model;
import com.tommyhasselman.termsconditions.model.Item;

import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotNull;
@SuppressWarnings("unused")
public class ItemTest {

    //TODO i need to figure out how to mock colour to test this
    //importing mockito into gradle build causing issues will fix when time free
/*
    private Item o1;
    private Item o2;
    private Item o3;
    private Item o69;

    @Before
    public void setUp() throws Exception {



            o1 = new Item(1);
            o2 = new Item(2);
            o3 = new Item(3);
            o69 = new Item(69);




    }
    //just checks all the objects can be constructed properly even when out of bounds complexity
    @Test
    public void testComplexity() {
        assertNotNull(o1);
        assertNotNull(o2);
        assertNotNull(o3);
        assertNotNull(o69);

    }
*/
}
